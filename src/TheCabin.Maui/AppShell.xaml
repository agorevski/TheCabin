<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TheCabin.Maui.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:TheCabin.Maui.Views"
    Shell.FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="False">

    <ShellContent
        Title="Story Selector"
        ContentTemplate="{DataTemplate views:StoryPackSelectorPage}"
        Route="StoryPackSelectorPage" />
    
    <!-- Game Screen -->
    <ShellContent
        Title="The Cabin"
        ContentTemplate="{DataTemplate views:MainPage}"
        Route="MainPage"
        IsVisible="False" />
    
    <!-- Inventory -->
    <ShellContent
        Title="Inventory"
        ContentTemplate="{DataTemplate views:InventoryPage}"
        Route="InventoryPage"
        IsVisible="False" />
    
    <!-- Settings -->
    <ShellContent
        Title="Settings"
        ContentTemplate="{DataTemplate views:SettingsPage}"
        Route="SettingsPage"
        IsVisible="False" />
    
    <ShellContent
        Title="Load Game"
        ContentTemplate="{DataTemplate views:LoadGamePage}"
        Route="LoadGamePage"
        IsVisible="False" />

</Shell>
